{
  "name": "@jlog/extension",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "package": "pnpm run build && rm -f jlog-extension.zip && cd dist && zip -qr ../jlog-extension.zip . && cd .. && echo \"Wrote apps/extension/jlog-extension.zip \u2014 upload this to the Chrome Web Store\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.280",
    "jsdom": "24",
    "typescript": "^5.7.2",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
